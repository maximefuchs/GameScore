<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Ecart buttons in addTarotActivity-->
    <style name="ButtonStyleDifferenceUnselected">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:background">@color/colorPrimaryLight</item>
    </style>

    <style name="ButtonStyleDifferenceSelected">
        <item name="android:textColor">@color/colorBlack</item>
    </style>

    <!-- Quit buttons -->
    <style name="ButtonStyleQuitYes">
        <item name="android:background">@color/colorNegative</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:text">@string/yes</item>
    </style>

    <style name="ButtonStyleQuitNo">
        <item name="android:background">@color/colorPositive</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:text">@string/no</item>
    </style>

    <!-- Restart buttons -->
    <style name="ButtonStyleRestartNo">
        <item name="android:background">@color/colorNegative</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:text">@string/no</item>
    </style>

    <style name="ButtonStyleRestartYes">
        <item name="android:background">@color/colorPositive</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:text">@string/yes</item>
    </style>

    <!-- EditText for score in AddGameBelote -->
    <style name="ScoreAddGameBelote">
        <item name="android:layout_width">0px</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:backgroundTint">@color/colorAccentTransparent2</item>
        <item name="android:gravity">center</item>
        <item name="android:hint">@string/hintBeloteScore</item>
        <item name="android:inputType">number</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textColorHint">@color/colorAccentTransparent2</item>
        <item name="android:textSize">22sp</item>
    </style>

</resources>
